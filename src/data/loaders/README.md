# data/loaders/

Funções de carregamento e parsing dos datasets (CSV/JSON em `public/data/`).
Devem retornar dados já tipados (ver `src/types/`) e prontos para os
componentes de gráfico — filtragem, agregação e limpeza acontecem aqui, não
dentro dos componentes visuais.
